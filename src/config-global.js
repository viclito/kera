import { paths } from './routes/paths';

// ROOT PATH AFTER LOGIN SUCCESSFUL
export const PATH_AFTER_LOGIN = paths.fpc.root; // as '/dashboard'
export const PATH_AFTER_LOGIN_ABP = paths.abp.root; // as '/dashboard'

export const PATH_BEFORE_LOGIN = paths.auth.login; // as '/dashboard'



export const PATH_AFTER_ADMIN_LOGIN = paths.admin_dashboard.root; // as '/dashboard'
